# キャラクターシステムセットアップガイド

## 📋 実装済みの機能

以下のファイルが作成されました：

1. ✅ `src/shared/Utils/CreatureTypes.luau` - 毒生物タイプの定義
2. ✅ `src/server/Components/VenomousCharacter.luau` - 毒生物キャラクターコンポーネント
3. ✅ `src/server/Services/PlayerManager.luau` - プレイヤー管理サービス
4. ✅ `src/server/init.server.luau` - サービス初期化

## 🎨 Roblox Studioでの作業手順

### ステップ1: 毒ヘビのモデルをツールボックスから取得

1. **Roblox Studioを開く**
   - Rojoサーバーが起動していることを確認
   - プロジェクトが接続されていることを確認

2. **ツールボックスを開く**
   - 左側のパネルから「ツールボックス」タブをクリック
   - または、`View > Toolbox` を選択

3. **毒ヘビのモデルを検索**
   - 検索バーに「snake」または「ヘビ」と入力
   - 適切な毒ヘビのモデルを探す
   - 例: "Snake", "Poison Snake", "Venomous Snake" など

4. **モデルを取得**
   - 気に入ったモデルをクリック
   - モデルがWorkspaceに配置されます

5. **モデルIDを確認**
   - モデルを選択
   - Propertiesパネルで「AssetId」または「SourceAssetId」を確認
   - または、モデルを右クリック > 「Copy Asset ID」でIDをコピー

### ステップ2: モデルIDをコードに設定

1. **ServerStorageにモデルを移動**
   - 取得したモデルを`ServerStorage`に移動
   - モデル名を「SnakeModel」などに変更（わかりやすい名前に）

2. **CreatureTypes.luauを更新**
   - `src/shared/Utils/CreatureTypes.luau`を開く
   - 取得したモデルIDを設定：
   ```lua
   CreatureTypes.MODEL_IDS = {
       Snake = 1234567890, -- ここに実際のモデルIDを設定
       -- ...
   }
   ```

3. **ファイルを保存**
   - 保存すると、Rojoが自動的にRoblox Studioに同期します

### ステップ3: モデルをロードする処理を実装

現在の`VenomousCharacter.luau`は基本的な構造のみです。
モデルを実際にロードして適用する処理を追加する必要があります。

**実装例**（`VenomousCharacter.luau`の`transformToCreature`関数を更新）：

```lua
function VenomousCharacter:transformToCreature()
	if not self.character then
		warn("Character is not initialized")
		return
	end
	
	-- モデルIDを取得
	local modelId = CreatureTypes.getModelId(self.creatureType)
	if not modelId then
		warn("Model ID not found for creature type:", self.creatureType)
		return
	end
	
	-- ServerStorageからモデルを取得
	local ServerStorage = game:GetService("ServerStorage")
	local modelTemplate = ServerStorage:FindFirstChild("SnakeModel")
	
	if not modelTemplate then
		warn("SnakeModel not found in ServerStorage")
		return
	end
	
	-- モデルを複製
	local creatureModel = modelTemplate:Clone()
	
	-- キャラクターの既存パーツを非表示（オプション）
	-- または、モデルをキャラクターの子として配置
	
	-- モデルをキャラクターに適用
	-- 注意: 実装方法はモデルの構造によって異なります
	creatureModel.Parent = self.character
	
	self.creatureModel = creatureModel
end
```

## 🧪 テスト手順

### テスト1: 基本的な動作確認

1. **Roblox Studioで「Play」をクリック**
2. **Outputウィンドウを確認**
   - エラーがないことを確認
3. **ゲーム内でプレイヤーを確認**
   - プレイヤーが毒ヘビに変身しているか確認
   - サイズが正しく設定されているか確認

### テスト2: サイズ変更の確認

1. **開発者コンソールを開く**（F9）
2. **以下のコマンドを実行**：
   ```lua
   local Players = game:GetService("Players")
   local Server = game.ServerScriptService.Server
   local Services = Server.Services
   local PlayerManager = require(Services.PlayerManager)
   
   local player = Players.LocalPlayer
   local character = PlayerManager:getCharacter(player)
   if character then
       character:setSize(2.0) -- サイズを2倍に
   end
   ```
3. **プレイヤーのサイズが変更されることを確認**

## 📝 実装の注意点

### モデルの構造について

ツールボックスから取得したモデルの構造によって、実装方法が異なります：

1. **単純なパーツの集合**
   - 各パーツをキャラクターに直接適用
   - サイズ調整が容易

2. **Riggedモデル（Humanoid付き）**
   - Humanoidを削除または無効化
   - パーツのみを適用

3. **アニメーション付きモデル**
   - アニメーションを保持するか削除するか検討

### パフォーマンスの考慮

- モデルは一度ロードして再利用
- 不要なパーツは削除
- コリジョンの設定を適切に行う

## 🔄 次のステップ

キャラクターシステムが完成したら：

1. ✅ タスク3: キャラクターシステム（進行中）
2. ⏳ タスク4: 移動システム（次）

移動システムでは、WASD移動とサイズに応じた速度調整を実装します。

